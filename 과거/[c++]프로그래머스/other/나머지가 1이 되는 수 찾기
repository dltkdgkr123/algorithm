// 답이 존재함은 보장된다고 써있으므로 예외처리x

#include <string>
#include <vector>

using namespace std;

int solution(int n) {
    
    for (int i = 1; i < n; i++)
    {
        if (n % i == 1)
        return i;
    }
}
