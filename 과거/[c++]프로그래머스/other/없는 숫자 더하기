#include <string>
#include <vector>

using namespace std;

int solution(vector<int> numbers) {
    int answer = 0;
    int fac = 9;

    while (fac)
        {answer += fac; fac--;}

    for (int i = 0; i < numbers.size(); i++)
        answer -= numbers[i];
    
    return answer;
}
